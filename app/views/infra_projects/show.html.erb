<p id="notice"><%= notice %></p>

<div class="row" style="height: 100px">
  <div class="col-4">
    <%= link_to 'Edit', edit_infra_project_path(@infra_project) %> |
    <%= link_to 'Back', infra_projects_path %>
    <p>
      <strong>Title:</strong>
      <%= @infra_project.title %>
    </p>

    <p>
      <strong>Status:</strong>
      <%= @infra_project.status %>
    </p>

    <p>
      <strong>Start date:</strong>
      <%= @infra_project.start_date %>
    </p>

    <p>
      <strong>End date:</strong>
      <%= @infra_project.end_date %>
    </p>

    <p>
      <strong>Desc:</strong>
      <%= @infra_project.desc %>
    </p>

    <ul>
      <% @layers.each do |layer| %>
        <li><%= layer.title %></li>
      <% end %>
    </ul>
  </div>
  <div class="col-8">
    <div id="mapid" style="height: 700px"></div>
  </div>
</div>

<script type="text/javascript" charset="utf-8">
    var mymap = L.map('mapid').setView([42.8746, 74.56], 13);

    L.tileLayer('https://api.tiles.mapbox.com/v4/{id}/{z}/{x}/{y}.png?access_token=pk.eyJ1IjoiZGFuY2hpa2ciLCJhIjoiY2sydWJjbHE2MHlxODNncHZycXRuOTB2OCJ9.4uXwJG8uQWH2SFYtBRlCLA', {
        attribution: 'Map data &copy; <a href="https://www.openstreetmap.org/">OpenStreetMap</a> contributors, <a href="https://creativecommons.org/licenses/by-sa/2.0/">CC-BY-SA</a>, Imagery © <a href="https://www.mapbox.com/">Mapbox</a>',
        maxZoom: 18,
        id: 'mapbox.streets',
        accessToken: 'your.mapbox.access.token'
    }).addTo(mymap);
</script>
